{
  "name": "seonwoo-care",
  "version": "1.0.0",
  "description": "선우케어 - 우리 아기 전용 케어 앱",
  "scripts": {
    "build:web": "mkdir -p www && cp index.html www/ && cp -r css www/ && cp -r js www/ && cp manifest.json www/ && cp sw.js www/",
    "cap:sync": "npm run build:web && npx cap sync android",
    "cap:build": "npm run cap:sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.3.0",
    "@capacitor/cli": "^8.3.0",
    "@capacitor/core": "^8.3.0"
  }
}
